# Core (always required)
httpx>=0.27.0
# PEP 440-correct version comparison for --outdated (falls back to a
# best-effort heuristic if absent)
packaging>=21.0

# Required on Python < 3.11 for TOML parsing (3.11+ uses stdlib tomllib)
tomli>=2.0; python_version < "3.11"

# Optional (additional lockfile formats):
#   pip install pyyaml         — pnpm-lock.yaml support
